## 1. CLI 骨架与计划契约

- [x] 1.1 重构当前 Node CLI 入口，补齐命令解析框架并提供 `exec` 与 `prepare-runtime` 两类主入口
- [x] 1.2 定义结构化 plan schema 与步骤类型约定，覆盖 `session`、`actions`、`gestures`、`assertions`、`artifacts` 与 `session.close`
- [x] 1.3 实现 plan 文件读取、schema 校验与错误输出，确保非法计划在执行前被拒绝
- [x] 1.4 设计并落地统一计划输入模型，支持计划文件与内联 JSON 两种执行输入
- [x] 1.5 新增 `prepare-runtime` 预热命令，用于提前准备 `uv`、托管 Python 和项目环境

## 2. Python 执行内核与共享分层

- [x] 2.1 在仓库内建立 Python 执行内核目录结构，形成 `domain`、`adapters/minium`、`support` 的基础分层
- [x] 2.2 抽取或迁移参考仓库中与 MCP 无关的配置、日志、产物管理和错误模型基础能力
- [x] 2.3 设计并实现基于 `uv --managed-python` 的私有运行时目录、缓存复用和跨平台定位策略，优先使用 3.14 且允许任意 3.11+ 版本，确保不污染用户全局环境
- [x] 2.4 建立 Node CLI 与 Python 内核之间的 JSON 进程通信协议，约定输入、输出与错误载荷格式
- [x] 2.5 实现执行内核启动与调用封装，确保 CLI 能通过 `uv` 稳定拉起 Python 子进程并消费执行结果

## 3. 会话与页面上下文能力

- [x] 3.1 实现基于项目路径启动小程序运行时的会话创建能力，包括开发者工具自动化端口准备与附着流程
- [x] 3.2 实现会话仓库与生命周期管理，维护 `session_id`、当前页面、最近截图、最近失败摘要和超时回收
- [x] 3.3 实现页面级上下文读取与会话内截图能力，并把结果纳入统一返回模型
- [x] 3.4 实现会话关闭与执行结束时的兜底清理，避免残留底层连接污染后续运行

## 4. 核心动作、等待与断言

- [x] 4.1 实现结构化定位器与元素查询能力，返回可序列化的查询结果摘要
- [x] 4.2 实现点击、输入和等待条件成立等核心自动化动作，并统一接入会话上下文
- [x] 4.3 实现页面路径、元素文本、元素可见性等结构化断言能力
- [x] 4.4 为动作失败、等待超时和断言失败接入统一错误模型与自动取证逻辑

## 5. 多触点手势与运行状态机

- [x] 5.1 在会话模型中实现 `active_pointers` 与 `latest_gesture_event`，支持跨步骤维持触点状态
- [x] 5.2 实现 `touch_start`、`touch_move`、`touch_tap`、`touch_end` 四类基础手势步骤
- [x] 5.3 实现手势目标解析，支持结构化定位器与页面绝对坐标两种目标形式
- [x] 5.4 实现两指以内的边界校验、手势失败取证与触点上下文摘要返回

## 6. AI-first 执行编排与测试运行

- [x] 6.1 实现结构化计划输入约束，并限制可执行内容只落在受支持的小程序自动化与测试语义内
- [x] 6.2 实现按步骤顺序执行 plan 的执行器，串联会话、动作、断言、手势与截图步骤
- [x] 6.3 实现运行级失败策略、步骤结果聚合与执行状态流转
- [x] 6.4 为非交互式脚本调用补齐稳定输出，确保相同 plan 可重复执行和对比

## 7. 运行摘要、产物与退出码

- [x] 7.1 实现按运行 ID 分层的产物目录结构，落盘摘要、截图和调试上下文
- [x] 7.2 实现终端可读的 run summary，同时输出结构化结果文件供脚本或 CI 消费
- [x] 7.3 实现与运行结果一致的退出码约定，区分成功、动作/断言失败、环境错误、计划错误和内部错误
- [x] 7.4 实现中文与英文输出切换，保证可读消息多语言而结构化字段稳定

## 8. 验证、示例与发布准备

- [x] 8.1 为 plan schema、CLI 命令解析和结果汇总补充单元测试
- [x] 8.2 为会话、动作、断言、手势与失败取证补充最小集成验证或 smoke tests
- [x] 8.3 验证基于 `uv` 的私有托管运行时在 macOS、Linux、Windows 三类平台上的缓存隔离、懒加载与全局环境无污染约束
- [x] 8.4 提供至少一个文件计划执行示例和一个内联 JSON 执行示例
- [x] 8.5 更新 README、安装说明和本地运行文档，明确该项目是 AI-first CLI 而非 MCP Server，并说明托管运行时只对当前 CLI 生效
