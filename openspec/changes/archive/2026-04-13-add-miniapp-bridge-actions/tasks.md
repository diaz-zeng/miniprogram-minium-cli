## 1. Plan And Spec Surface

- [x] 1.1 定义高优先级桥接步骤命名空间与输入输出 schema，覆盖存储、路由、应用上下文、设置/授权、剪贴板与反馈 UI。
- [x] 1.2 定义中优先级桥接步骤命名空间与输入输出 schema，覆盖位置、媒体、文件、设备交互、登录会话与订阅消息。
- [x] 1.3 更新计划校验逻辑，确保桥接步骤仅接受白名单动作与受支持参数形状。

## 2. Runtime And Execution

- [x] 2.1 在 Python 动作模型与执行引擎中新增桥接步骤分发逻辑。
- [x] 2.2 在 Minium 运行时适配层实现高优先级桥接动作，并统一同步调用与错误摘要。
- [x] 2.3 在 Minium 运行时适配层实现中优先级桥接动作，并补齐异步结果等待与超时处理。
- [x] 2.4 为桥接动作补充统一的结构化错误上下文，包括步骤类型、底层方法名、关键参数和当前页面路径。
- [x] 2.5 为需要开发者自有 AppID 的桥接步骤增加 `touristappid` 检测与跳过语义，并在运行摘要中记录跳过原因。

## 3. Demo Scenarios And Regression Plans

- [x] 3.1 在示例小程序中增加高优先级桥接能力对应的页面场景、可观测状态和场景说明文档。
- [x] 3.2 在示例小程序中增加中优先级桥接能力对应的页面场景、可观测状态和场景说明文档。
- [x] 3.3 为高优先级桥接能力新增 regression plan，并标注是否依赖开发者自有 AppID。
- [x] 3.4 为中优先级桥接能力新增 regression plan，并为受限场景补充 `touristappid` 跳过预期。

## 4. Documentation And Tests

- [x] 4.1 更新英文与中文 API 文档，列出新增桥接步骤、跳过语义、输入输出字段与失败语义。
- [x] 4.2 增加高优先级桥接动作的单元测试与 smoke 测试样例。
- [x] 4.3 增加中优先级桥接动作的单元测试与 smoke 测试样例，重点覆盖异步场景、失败路径与 `touristappid` 跳过路径。
- [x] 4.4 更新仓库内 `skills/miniprogram-minium-cli/` 的相关指导文档，补齐桥接步骤的 plan 编写、执行与结果分析说明。

## 5. Acceptance

- [x] 5.1 完成 `acceptance-checklist.md` 中列出的全部验收 plan 执行与结果核对；仅当清单中的所有条目都已勾选完成后，本任务才可标记为完成。
